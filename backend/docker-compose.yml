services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: equireign
      POSTGRES_USER: equireign
      POSTGRES_PASSWORD: change-me
    ports:
      - "5432:5432"
    volumes:
      - equireign-postgres:/var/lib/postgresql/data
volumes:
  equireign-postgres:
