name: Temporary lockfile sync (disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Lockfile already synchronized; remove this temporary workflow before merge."
